type User @model {
  email: Email! @unique
  name: String
  avatar: URL
}

type Episode @model {
  title: String!
  slug: String! @unique
  excerpt: String
  content: String
  thumbnail: URL
  videoUrl: URL
}
